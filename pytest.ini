[pytest]
# Unit tests for the minimal Wheeler-graph repair feature. Run with the python3 interpreter,
# which has BOTH pytest and z3:  python3 -m pytest
# (Only repair/tests is collected; the repo's other standalone main()-scripts are not tests.)
testpaths = repair/tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
